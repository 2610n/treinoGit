
para ver os status do que foi modificado no controle de versão 
comando : git status

para adicionar no controle de versão 
 comando : git add "nome do arquivo " ou git add . (se caso tiver mais de um arquivo)


para salvar a modificação 
comando : git commit -m "defnir  o nome da modificação "

push
antes de fazer um push(subir para nuvem)
tem  criar um repositório no github antes de procegit com o comando (push)

feito o repositório entre nele depois pegue o link dele copie

e no terminal do git digite o comando 
git remote add origin  link copiado

após tudo isso 
utilize o comando push para subir para sua nuvem 
se der algum problema tipo (se pergutar para qul repositório manda esteja atento com comando que aparece com o aviso )geralmente quando manda para nuven e
o rpositório manda para bransh principal no caso (master)

comando git reflog (mostr o historico de comit junto com id se caso quiser retorna para um estado anterior)

commit(versão)
como voltar a uma versão anterior do seu código
comando: git reset --hard ID do comiit que vc quer voltar;

Branch (caminhos diferente durante o processo de desenvolvimento)

marge quando vc mescla seu código de uma branch secundaria  com o branch (master)
comando para saber as branch disponívei  : git branch
comando para criar uma nova branch : git branch nome da branch

para identificar qual a branch que está sendo utilizada basta digitar o comando :git branch  e verificar qual branch esta com cor verde

para mudar de branch basta usar o comando :git checkout e o nome da branch que deseja usar.

quando você cria uma branch nova e quer subir ele para nuvem
o git pede para vc executar um comando primeiro .(ele tepassa o comando via mensagem do terminal na hora de dar um push)




